services:
  - type: web
    name: tbl-grocery-scanner
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn elegant.app:app --workers 2 --worker-class uvicorn.workers.UvicornWorker --bind 0.0.0.0:$PORT
    pythonVersion: 3.10.0
